#include <stdio.h>

int main()
{
    
 float poids_ble = 6.5870;
 double production = 0.849479521989;
 unsigned int nb_cases;

 nb_cases = 0;
 production*=1000000; //passage de Tonnes en grammes//

 while(poids_ble < production)
 {
   poids_ble *= 2;
   nb_cases++;

}printf("%u", nb_cases);
}

/*do 
{
  poids_ble *= 2;
   nb_cases++;

} while(poids_ble < production);
*/

/*

for(nb_cases = 0;poids_ble < production; nb_cases++){
  poids_ble *= 2
}

*/
